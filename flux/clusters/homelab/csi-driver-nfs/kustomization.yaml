apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

resources:
  - ../../../base/csi-driver-nfs
  - storageclass.yaml

patches:
  - patch: helmrelease-patch.yaml
    target:
      kind: HelmRelease
      name: csi-driver-nfs
      namespace: kube-system
